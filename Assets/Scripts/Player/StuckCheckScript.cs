using UnityEngine;

public class StuckCheckScript : MonoBehaviour
{
    /*private void OnTriggerStay2D(Collider2D other)
    {

        if (other.gameObject.layer == (int)Utils.Enums.ObjectLayers.Wall)
        {
            Debug.Log("Player is stuck INSIDE the wall.");
            LogicManager sup = GameObject.FindGameObjectWithTag(Utils.Const.LOGIC_MANAGER_TAG).GetComponent<LogicManager>();
            sup.GameOverAmbient();
        }
    }*/
}
